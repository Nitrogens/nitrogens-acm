#include <stdio.h>
int main(void)
{
	int T, S;
	
	scanf("%d", &T);
	
	if(T <= 3500)
	{
		S = T;
	}
	
	if( (T > 3500) && (T <= 4955) )
	{
		S = (T - 105) / 0.97f;
	}
	
	if( (T > 4955) && (T <= 7655) )
	{
		S = (T - 455) / 0.9f;
	}
	
	if( (T > 7655) && (T <= 11255) )
	{
		S = (T - 1255) / 0.8f;
	}
	
	if( (T > 11255) && (T <= 30755) )
	{
		S = (T - 1880) / 0.75f;
	}
	
	if( (T > 30755) && (T <= 44755) )
	{
		S = (T - 3805) / 0.7f;
	}
	
	if( (T > 44755) && (T <= 61005) )
	{
		S = (T - 6730) / 0.65f;
	}
	
	if(T > 61005)
	{
		S = (T - 15080) / 0.55f;
	}
	
	printf("%d", S);
	
	return 0;
}
