/*
	
	
	printf("%d\n", hero_hp[0]);
	
	if (summon_sum[0] == 0)
	{
		printf("%d\n", summon_sum[0]);
	}
	else
	{
		printf("%d", summon_sum[0]);
		for(i=0; i<summon_sum[0]; i++)
		{
			printf(" [%d]%d-%d", i, summon_attack[0][i], summon_hp[0][i]);
		}
		printf("\n");
	}
	
	printf("%d\n", hero_hp[1]);
	
	if (summon_sum[1] == 0)
	{
		printf("%d\n", summon_sum[1]);
	}
	else
	{
		printf("%d", summon_sum[1]);
		for(i=0; i<summon_sum[1]; i++)
		{
			printf(" [%d]%d-%d", i, summon_attack[1][i], summon_hp[1][i]);
		}
		printf("\n");
	}
	*/
